export enum PosType {
    Long = 0,
    Short = 1,
}
export enum LimitType {
    Limit = 0,
    Stop = 1,
}
export enum StopPositionType {
    StopLoss = 0,
    TakeProfit = 1,
}
export enum LimitOrderSate {
    Pending = "0",
    Executed = "1",
}
