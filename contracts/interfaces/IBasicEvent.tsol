pragma ever-solidity ^0.62.0;


interface IBasicEvent {
  enum Vote {
    Reserved,
    Empty,
    Confirm,
    Reject
  }
  enum Status {
    Initializing,
    Pending,
    Confirmed,
    Rejected
  }

  event Reject(uint256 relay);
  event Closed();
}
